{
  "name": "commerce-students-ai-proxy",
  "version": "1.0.0",
  "type": "module",
  "description": "Secure proxy that holds GEMINI_API_KEY server-side so GitHub Pages client never exposes secret",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
